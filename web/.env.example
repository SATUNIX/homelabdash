# Base URL of the authentication gateway (e.g. https://auth.example.com).
# When set, a "Gateway" link appears in the header and side navigation.
# Leave empty to hide the link entirely.
VITE_AUTH_GATEWAY_URL=
